# ClassFlow 시스템 테스트 가이드 (Test Guide)

ClassFlow(사내 강사 수업 일지 관리 시스템)의 원활한 기능 검증 및 E2E 테스트를 위한 사용자 역할 구분, 테스트 계정, 그리고 핵심 기능 흐름 명세서입니다.

---

## 👥 1. 사용자 구분 및 권한 매트릭스

| 구분 | **강사 (INSTRUCTOR)** | **관리자 (ADMIN)** |
| :--- | :--- | :--- |
| **테스트 계정** | `inst1` ~ `inst6` / `pass1234!` | `admin` / `admin1234!` |
| **로그인 후 첫 화면** | `#/instructor/logs` (수업 일지 작성) | `#/admin/logs` (전체 일지 관리) |
| **수업 일지 권한** | **본인 작성 일지만** 등록/조회/수정/삭제 | **모든 강사의 일지** 조회/수정/삭제 및 **엑셀 다운로드** |
| **강사/과정 조회** | 목록 및 검색 조회 (Read-Only) | 목록 및 검색 조회 + **수정/삭제 버튼 노출** |
| **내 프로필** | 본인 연락처/소개글 수정 가능 | 본인 프로필 조회 |
| **마스터 관리** | ❌ 접근 불가 (URL 직접 접근 시 자동 차단) | ⭕ **강사 계정 신규 발급 / 과정 개설 / 수정 / 삭제** |

---

## 🔄 2. 핵심 기능 테스트 시나리오 (End-to-End Flow)

```
[시나리오 1: 강사 모드]
로그인 (inst1) ➔ 일지 작성 (과정선택/시간입력) ➔ 캘린더 점 마킹 & 리스트 확인 ➔ 프로필 수정

[시나리오 2: 관리자 모드]
로그인 (admin) ➔ 전체 일지 다조건 검색 ➔ 엑셀(CSV) 다운로드 ➔ 마스터 관리 (신규 강사/과정 생성)

[시나리오 3: 보안/권한 제어]
비로그인 접근 차단 ➔ 강사 계정으로 관리자 URL(/admin/*) 접근 시 자동 튕김 차단
```

---

### 🔹 시나리오 A. 강사(Instructor) 일상 업무 테스트

1. **로그인**:
   * 아이디: `inst1`, 비밀번호: `pass1234!` 입력 (로그인 화면의 '강사 자동입력' 버튼 활용 가능)
   * 로그인 성공 시 `#/instructor/logs`로 자동 이동

2. **수업 일지 작성 (`#/instructor/logs`)**:
   * **수업 일자**: 날짜 선택 (기본값 오늘)
   * **과정명**: 셀렉트 박스에서 등록된 과정을 선택하거나, 원하는 과정명을 직접 텍스트로 타이핑하여 엔터 (태그 모드)
   * **수업 시간**: 시작 시간 ~ 종료 시간 설정 (예: `14:00` ~ `16:30`) ➔ 총 수업 시간(`2.5h`, 0.5h 단위) 자동 계산 확인
   * **참여 인원 및 수업 내용**: 인원(예: `15명`), 수업 진도 및 실습 내용 입력 후 **[일지 등록]** 클릭

3. **작성 내역 확인 & 인터랙션**:
   * 우측 **리스트 뷰**에 새로 등록된 일지가 상단에 즉시 추가되는지 확인
   * **[캘린더]** 탭 전환 ➔ 해당 날짜에 **점 마킹(Dot)** 표시 확인
   * 날짜의 점 마킹 클릭 ➔ 일지 상세 팝오버 확인
   * 일지 **[수정]** 클릭 ➔ 수정 모달에서 내용 변경 후 저장 확인
   * 일지 **[삭제]** 클릭 ➔ 삭제 확인 팝업(`Popconfirm`) 후 삭제 완료 확인

4. **내 프로필 수정 (`#/instructor/profile`)**:
   * 연락처(휴대폰), 간단 소개글 입력 후 **[저장하기]** 클릭
   * 상단 GNB(네비게이션 바) 우측 아바타/이름 영역에 수정된 정보가 즉시 동기화되는지 확인

---

### 🔹 시나리오 B. 관리자(Admin) 통합 운영 및 데이터 추출 테스트

1. **로그인**:
   * 아이디: `admin`, 비밀번호: `admin1234!` 입력
   * 로그인 성공 시 `#/admin/logs`로 자동 이동

2. **전체 일지 관리 (`#/admin/logs`)**:
   * **다조건 필터 검색**:
     * 기간 선택 (RangePicker)
     * 강사 선택 (특정 강사 또는 전체 강사)
     * 과정 선택 (특정 과정 또는 전체 과정)
     * **[검색]** 버튼 클릭 ➔ 필터링된 결과 및 총 건수 뱃지 확인
   * **엑셀 다운로드**:
     * **[엑셀 다운로드]** 버튼 클릭
     * 필터 조건이 반영된 `classflow_logs_YYYYMMDD.csv` 파일 다운로드 확인
     * 엑셀 파일 오픈 시 한글(UTF-8 BOM) 깨짐 없이 정상 출력되는지 확인
   * **관리자 권한 수정/삭제**:
     * 타 강사가 작성한 일지의 [수정] 및 [삭제]가 정상 작동하는지 확인

3. **마스터 관리 (`#/admin/master`)**:
   * **강사 계정 신규 발급**:
     * 로그인 아이디(예: `test_inst`), 초기 비밀번호(예: `pass1234!`), 강사명, 소속 입력 후 **[계정 발급]** 클릭
     * 하단 강사 계정 목록에 즉시 추가되는지 확인
   * **신규 교육 과정 개설**:
     * 과정명(예: `Kubernetes 실무 마스터`), 과정 설명 입력 후 **[과정 등록]** 클릭
     * 하단 과정 목록 및 과정 소개 페이지에 즉시 추가되는지 확인

4. **신규 발급 계정 로그인 검증**:
   * 상단 프로필 드롭다운에서 **[로그아웃]** 클릭
   * 새로 발급한 강사 계정(`test_inst` / `pass1234!`)으로 로그인하여 강사 화면으로 정상 진입되는지 확인

---

### 🔹 시나리오 C. 권한 및 보안 검증 (Security Guard)

1. **비로그인 보호 (Auth Guard)**:
   * 로그아웃 상태에서 브라우저 주소창에 `http://localhost:5173/#/instructor/logs` 또는 `#/admin/logs` 직접 입력
   * 자동으로 `#/login` 페이지로 튕기는지 확인

2. **역할 권한 제어 (Role-based Guard)**:
   * 강사(`inst1`)로 로그인한 상태에서 주소창에 관리자 전용 URL인 `#/admin/master` 또는 `#/admin/logs` 직접 입력
   * 접근이 차단되고 강사 기본 페이지(`#/instructor/logs`)로 자동 리다이렉트되는지 확인

3. **토큰 만료 및 변조 방어**:
   * 개발자 도구(F12) ➔ Application ➔ Local Storage에서 `classflow_access_token` 값을 임의로 변조
   * 페이지 새로고침 시 401 감지 후 세션이 안전하게 초기화되며 `#/login`으로 이동하는지 확인
